{
  "mcpServers": {
    "linux-tools": {
      "command": "uvx",
      "args": [
        "linux-mcp-server"
      ],
      "env": {
        "LINUX_MCP_ALLOWED_LOG_PATHS": "/var/log/messages,/var/log/lastlog"
      }
    }
  }
}
